<div class="container">
  <div class="row" >
    <div class="wrappser col-md-6 hidden-sm hidden-xs">
      <%= render 'devise/top' %>
    </div>
    <div class="wrapper col-md-offset-1 col-md-4 col-sm-offset-2 col-sm-8 col-xs-offset-1 col-xs-10">
      <h1 class="logotop">InstantAlubum</h1>

      <h2>ユーザー登録</h2>

      <%= alert %>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= devise_error_messages! %>

        <div class="field">
          <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "メールアドレス" %>
        </div>

        <div class="field">
          <%= f.text_field :name, class: "form-control", placeholder: "名前" %>
        </div>

        <div class="field">
          <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> 文字以上)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder: "パスワード" %>
        </div>

        <div class="field">
          <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control", placeholder: "パスワード（確認用）" %>
        </div>

        <div class="actions">
          <%= f.submit "ユーザー登録", class: "btn btn-primary btn-block" %>
        </div>
      <% end %>
        <p>登録後、メールアドレスに確認メールをお送りします。アカウントを有効にしてからログインしてください。</p>
       <hr>
      <div>
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>